import React, { Component } from "react";
import { StyleSheet, View, Text } from "react-native";
import DailyCalendarBlock from "./components/DailyCalendarBlock";

function Index(dayProp) {
  console.log(dayProp)
  return (
    <View style={styles.container}>
=   
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    borderRadius: 20,
    width: 179,
    height: 179,
    marginLeft: 16,
    marginTop: 10 
  },
  row: {
    
  }
});

export default Index;
